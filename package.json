{
  "name": "mindit-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional da mindit. — IA sob demanda para empresas.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node build.mjs",
    "serve": "node scripts/serve-dist.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  },
  "engines": {
    "node": ">=18"
  }
}
